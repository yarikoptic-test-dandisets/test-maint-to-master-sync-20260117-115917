name: CI

on:
  pull_request:
  push:
    branches: [master, maint]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: |
          echo "Running tests..."
          cat README.md
          echo "✅ Tests passed"
